# .NET
bin/
obj/
*.user

# Visual Studio local state (churns per session; not shared)
.vs/

# Node
node_modules/
web/dist/

# Bake output (derived data — rebakeable from source).
# Anchored to repo root so it never matches source dirs like src/.../Tiles/.
/tiles/
/staging/

# Env / local
*.local
.DS_Store

# Built frontend copied into the server for single-origin deploy
/src/Colossus.Server/wwwroot/

# Publish output + deploy zips (assembled per DEPLOY.md, never source)
/publish/
/publish-x86/
/*.zip

# Temporary perf harness (stutter measurements; loaded manually)
/web/public/__perf-harness.js
